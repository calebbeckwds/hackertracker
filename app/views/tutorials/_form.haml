= form_for [@tutorial.unique_item, @tutorial] do |f|
  - if @tutorial.errors.any?
    #error_explanation
      %h2
        = pluralize(@tutorial.errors.count, "error")
        prohibited this tutorial from being saved:
      %ul
        - @tutorial.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :body
    %br/
    = f.text_area :body
  .field
    = f.label :url
    %br/
    = f.text_area :url
  .actions
    = f.submit
