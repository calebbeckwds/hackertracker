<%= form_for([@caveat.unique_item, @caveat]) do |f| %>
  <% if @caveat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caveat.errors.count, "error") %> prohibited this caveat from being saved:</h2>

      <ul>
      <% @caveat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
