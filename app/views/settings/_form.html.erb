<%= form_for(@setting) do |f| %>
  <% if @setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setting.errors.count, "error") %> prohibited this setting from being saved:</h2>

      <ul>
      <% @setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :paypal_user %><br />
    <%= f.text_field :paypal_user %>
  </div>
  <div class="field">
    <%= f.label :paypal_pass %><br />
    <%= f.text_field :paypal_pass %>
  </div>
  <div class="field">
    <%= f.label :paypal_signature %><br />
    <%= f.text_field :paypal_signature %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
