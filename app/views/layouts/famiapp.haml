!!!
%html
  %head
    %title Familab Unique Item Tracker
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
%body
  .container-fluid
    %br/
    - if flash[:error]
      .alert.alert-error= flash[:error]
    - if flash[:alert]
      .alert.alert-error= flash[:alert]
    - if flash[:notice]
      .alert.alert-info= flash[:notice]
    = yield

  %br/
  %hr/
  %br/
